/*
to create an object

let objName = {
    key: valueOfKey, (comma!!)
    ex. lastName: "Dragona",
    -- can use any data type (#, boolean, string, etc.)

    last keyvalue pair = NO COMMA AFTER IT
}
in console, you can just type the name of the object (ex. objName) and it will print it all out wiht keys
or to get specific val -- objName.key or objName["key"]
*/

let myFish = {

    color: "blue and green",
    size: "small",
    age: 1,
    name: "fishy",
    happy: true
}
console.log("Color of my fish:");
console.log(myFish.color);
console.log("Type in myFish to see all values stored in object!");