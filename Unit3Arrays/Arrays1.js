let bobsList = [21, "bob", 'A', 'B']; // you can out ANYTHING in it
// to access array

console.log(bobsList);
console.log(bobsList.toString());

// Or to access from terminal --  type in console "bosList" (i.e. name of array) OR bobsList.toString(); 

